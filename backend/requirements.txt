# ── Web framework ─────────────────────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.3
python-multipart==0.0.9        # required for file uploads

# ── Image processing  (Member 5) ──────────────────────────
Pillow==10.4.0
opencv-python-headless==4.10.0.84
rembg                  # background / distraction removal
mediapipe             # body landmark detection for garment isolation

# ── AI / Watson  (Member 3 & 4) ───────────────────────────
ibm-watson==11.2.0              # Watson Assistant SDK
ibm-cloud-sdk-core==3.26.1
requests>=2.32.4              # HTTP calls to watsonx.ai REST API

# ── Utilities ─────────────────────────────────────────────
python-dotenv==1.0.1           # load .env file
pydantic==2.8.2                # data validation (bundled with FastAPI but pinned)
pydantic-settings==2.4.0       # BaseSettings for config.py
aiofiles==23.2.1               # async file I/O for serving generated files
onnxruntime>=1.18.0              # required by rembg for model inference
gradio-client==0.16.1          # required for Trellis 3D API
